# 更新日志

## v1.2

- **新增 WebSocket 实时推送**：改用 `subscribe_trigger`，由服务端按 `entity_id` 过滤，
  延迟从秒级降到百毫秒级（局域网约 50~150 ms）
- 实时断线自动重连，指数退避 1→2→4→…→30 秒
- 重连期间自动降级为 REST 轮询，列表不中断
- 实时在线时每 60 秒做一次 REST 对账，兜住重连空档
- 提示音增加 2 秒最小间隔节流（实时推送下事件逐条到达，不节流会连环炸响）
- 设置页新增「实时推送」开关
- 标题栏显示当前通道：`⚡ 实时` / `⏳ 连接中` / `⏱ 轮询`

- 修复：**同一条变化会重复出现**。实时推送（WebSocket）的 `last_changed` 来自内存里
  的状态机、带微秒，而 REST 历史接口读的数据库里微秒已被抹掉（HA core PR #71704
  里为此专门做了 `floored_timestamp` 兼容）。两者字符串不同 → 去重失效，
  表现为每条实时变化在约 60 秒后的对账轮询时多出一条一模一样的记录。
  改用「**秒级时间戳 + 值**」作为去重键，可同时兼容抹微秒、截断到毫秒等不同数据库行为
## v1.1

- 新增提示音（系统通知铃声，跟随筛选结果，首次历史回溯不响）
- 刷新间隔改为可配置，默认由 3 秒降到 2 秒
- 修复：窗口内无变化时会被一直判定为「首次加载」，导致重复全量拉取；
  叠加提示音后还会表现为「永远不响」

## v1.0

- 首个可用版本
- 基于 REST 历史接口的增量轮询
- 关键词黑白名单筛选（开关切换模式）
- 新记录自动置顶
